version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm --prefix todo-app ci
    build:
      commands:
        - npm --prefix todo-app run build
  artifacts:
    baseDirectory: todo-app/build
    files:
      - '**/*'
  cache:
    paths:
      - todo-app/node_modules/**/*
